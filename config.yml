# -------------------------------------------------------------------
# JIRA CONFIGURATION
# -------------------------------------------------------------------
jira:
  url: "https://senagrun.atlassian.net"
  
  # GÜVENLİK NOTU:
  # Email ve API Token bilgileri güvenlik nedeniyle bu dosyaya YAZILMAMALIDIR.
  # Bu bilgiler aşağıdaki ortam değişkenleri (Environment Variables) üzerinden okunur:
  # - JIRA_EMAIL
  # - JIRA_TOKEN

# -------------------------------------------------------------------
# BITBUCKET CONFIGURATION
# -------------------------------------------------------------------
bitbucket:
  workspace: "senagrun-dev"
  repo_slug: "ecommerce-platform"
  
  # NOT: Bitbucket API erişimi için gerekirse BITBUCKET_TOKEN 
  # ortam değişkeni kullanılabilir.
